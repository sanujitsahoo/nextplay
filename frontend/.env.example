# Next.js Environment Variables
# Copy this file to .env.local for local development

# Backend API URL (optional - defaults to http://localhost:8000)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Instructions:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Update NEXT_PUBLIC_API_URL if your backend runs on a different port/URL
# 3. For production, set this via your hosting platform's environment variables
# 4. Never commit .env.local to version control
